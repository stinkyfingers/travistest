stages:
  - test

sudo: required

jobs:
  include:
    - stage: test
      sudo: required
      language: go
      go: 1.16.x
      dist: xenial
      before_install: ./scripts/install_script.sh $TRAVIS_COMMIT
